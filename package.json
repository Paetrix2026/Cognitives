{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "preinstall": "node -e \"const fs=require('node:fs'); for (const f of ['package-lock.json','yarn.lock']) { try { fs.unlinkSync(f); } catch {} } const ua=process.env.npm_config_user_agent||''; if (!ua.startsWith('pnpm/')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "dev": "node ./scripts/dev.mjs",
    "start": "node --enable-source-maps ./Backend/dist/index.mjs",
    "chain": "node ./scripts/local-chain.mjs",
    "chain:node": "pnpm --filter ./contracts run node",
    "chain:deploy": "pnpm --filter ./contracts run deploy:local",
    "chain:verify": "pnpm --filter ./contracts run verify:local",
    "contracts:compile": "pnpm --filter ./contracts run compile",
    "contracts:test": "pnpm --filter ./contracts run test",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./frontend\" --filter \"./Backend\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@rollup/rollup-win32-x64-msvc": "^4.60.2",
    "@tailwindcss/oxide-win32-x64-msvc": "^4.2.4",
    "lightningcss-win32-x64-msvc": "^1.32.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
