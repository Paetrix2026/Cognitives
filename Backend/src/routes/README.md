# Routes (API)

This folder contains API route modules.

## What to do here
- Add new endpoint files (one feature per file).
- Export and register routes in `index.ts`.
- Keep handlers thin; move business logic to `../services`.

## Start full project (from repo root)
```bash
pnpm dev
```
